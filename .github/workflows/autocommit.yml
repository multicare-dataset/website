name: Keep Streamlit Alive

on:
  workflow_dispatch:

  schedule:
    - cron: "0 */8 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit app
        run: |
          curl -L -s https://clinical-cases.streamlit.app/ > /dev/null
